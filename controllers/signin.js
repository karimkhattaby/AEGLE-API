// @ts-nocheck

const handleSigninPatient = (req, res, db) => {
};

const handleSigninDoctor = (req, res, db) => {
};

module.exports = {
    handleSigninPatient: handleSigninPatient,
    handleSigninDoctor: handleSigninDoctor
};